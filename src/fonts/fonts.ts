import {
  Outfit
} from "next/font/google";


export const Outfit_font = Outfit({
  subsets: ["latin"],
  display: "swap",
  weight: "600",
});

export const Outfit_body = Outfit({
  subsets: ["latin"],
  display: "swap",
  weight: "300",
});